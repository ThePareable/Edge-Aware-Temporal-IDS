Edge-Aware-Temporal-IDS

Minimal repo layout for reproducing the edge-aware temporal IDS experiments.

See `configs/` for class lists and metadata and `src/` for implementation.

Note: Large binary artifacts (.pkl, .pt, datasets) are intentionally omitted — see .gitignore.